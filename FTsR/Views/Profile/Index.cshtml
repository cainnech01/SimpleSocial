@model UserModel
@{
    ViewBag.Title = "Список пользователей";
}
<div id="profile_rounded" class="rounded-top rounded-3 text-white d-flex flex-row justify-content-center" style="background-color: #000; height:200px;">
    <div class="ms-4 mt-5 d-flex flex-column" style="width: 150px;">
        @if (Model.ProfilePicture != null)
        {
            <img src="data:image/jpeg;base64,@(Convert.ToBase64String(Model.ProfilePicture))"
                 alt="Generic placeholder image" class="rounded-circle img-fluid img-thumbnail mt-4 mb-2"
                 style="width: 150px; z-index: 1; height: 180px;">
        }
        <a asp-action="Edit" asp-route-id="@Model.Id" type="button" data-mdb-button-init data-mdb-ripple-init class="btn btn-outline-dark" data-mdb-ripple-color="dark"
           style="z-index: 1;">
            Edit profile
        </a>
    </div>
    <div class="ms-3 flex-column" style="margin-top: 130px;">
        <h5>@Model.UserName</h5>
        <p>New York</p>
    </div>
</div>
<div class="p-4 text-black" style="background-color: #f8f9fa;">
    <div class="d-flex justify-content-end text-center py-1">
        <div>
            <p class="mb-1 h5">253</p>
            <p class="small text-muted mb-0">Photos</p>
        </div>
        <div class="px-3">
            <p class="mb-1 h5">1026</p>
            <p class="small text-muted mb-0">Followers</p>
        </div>
        <div>
            <p class="mb-1 h5">478</p>
            <p class="small text-muted mb-0">Following</p>
        </div>
    </div>
</div>
<div class="card-body p-4 text-black">
</div>
@* <table class="table">
    <tr><th>Email</th><th>Год</th><th></th></tr>
    @foreach (var user in Model)
    {
        <tr>
            <td>@user.Email</td>
            <td>
                @if (user.ProfilePicture != null)
                {
                    <img style='width:80px; height:60px;' src="data:image/jpeg;base64,@(Convert.ToBase64String(user.ProfilePicture))" />
                }
            </td>
            <td>@user.Year</td>
            <td>
                <form asp-action="Delete" asp-route-id="@user.Id" method="post">
                    <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@user.Id">Изменить</a>
                    <button type="submit" class="btn btn-sm btn-danger">
                        Удалить
                    </button>
                </form>
            </td>
        </tr>
    }
</table> *@